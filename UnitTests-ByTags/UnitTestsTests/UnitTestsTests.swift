//
//  UnitTestsTests.swift
//  UnitTestsTests
//
//  Created by Alex Trott on 23/05/2021.
//

import XCTest
@testable import UnitTests

class LoginTestsTests: XCBaseTestCase {

    override var category: XCTestable { .login }

    func testExample() throws {

        // given
        let something = "ABC"

        // when
        let result = something.lowercased()

        // then
        XCTAssertEqual(result, "abc")
    }

    func testExample1() throws {

        // given
        let something = "ABC"

        // when
        let result = something.lowercased()

        // then
        XCTAssertEqual(result, "abc")
    }

    func testExample2() throws {

        // given
        let something = "ABC"

        // when
        let result = something.lowercased()

        // then
        XCTAssertEqual(result, "abc")
    }
}

class RegistrationTestsTests: XCBaseTestCase {

    override var category: XCTestable { .registration }

    func testExample() throws {

        // given
        let something = "ABC"

        // when
        let result = something.lowercased()

        // then
        XCTAssertEqual(result, "abc")
    }

    func testExample1() throws {

        // given
        let something = "ABC"

        // when
        let result = something.lowercased()

        // then
        XCTAssertEqual(result, "abc")
    }

    func testExample2() throws {

        // given
        let something = "ABC"

        // when
        let result = something.lowercased()

        // then
        XCTAssertEqual(result, "abc")
    }
}

class Feature1TestsTests: XCBaseTestCase {

    override var category: XCTestable { .feature1 }

    func testExample() throws {

        // given
        let something = "ABC"

        // when
        let result = something.lowercased()

        // then
        XCTAssertEqual(result, "abc")
    }

    func testExample1() throws {

        // given
        let something = "ABC"

        // when
        let result = something.lowercased()

        // then
        XCTAssertEqual(result, "abc")
    }

    func testExample2() throws {

        // given
        let something = "ABC"

        // when
        let result = something.lowercased()

        // then
        XCTAssertEqual(result, "abc")
    }
}

class Feature2TestsTests: XCBaseTestCase {

    override var category: XCTestable { .feature2 }

    func testExample() throws {

        // given
        let something = "ABC"

        // when
        let result = something.lowercased()

        // then
        XCTAssertEqual(result, "abc")
    }

    func testExample1() throws {

        // given
        let something = "ABC"

        // when
        let result = something.lowercased()

        // then
        XCTAssertEqual(result, "abc")
    }

    func testExample2() throws {

        // given
        let something = "ABC"

        // when
        let result = something.lowercased()

        // then
        XCTAssertEqual(result, "abc")
    }
}

class Feature3TestsTests: XCBaseTestCase {

    override var category: XCTestable { .feature3 }

    func testExample() throws {

        // given
        let something = "zxc"

        // when
        let result = something.lowercased()

        // then
        XCTAssertEqual(result, "abc")
    }

    func testExample1() throws {

        // given
        let something = "zxc"

        // when
        let result = something.lowercased()

        // then
        XCTAssertEqual(result, "abc")
    }

    func testExample2() throws {

        // given
        let something = "zxc"

        // when
        let result = something.lowercased()

        // then
        XCTAssertEqual(result, "abc")
    }
}
